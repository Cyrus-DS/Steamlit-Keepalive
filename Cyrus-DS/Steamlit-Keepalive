name: Keep Streamlit Awake

on:
  schedule:
    - cron: "17 */6 * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Streamlit App
        run: |
          curl -L --silent --show-error --fail "https://ndundu-finance-appgit-5xrzkytijl5kizazc5uxav.streamlit.app" > /dev/null
